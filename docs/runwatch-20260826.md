# State — 2026-08-26 03:45 UTC: Run C staged on the symmetric-merge path

Follows `runwatch-20260825.md` (Run A control, Run B abort, Run B2). Written after
reconciling this session's view with the overnight sessions' work, and after
corroborating the findings in `/opt/cassandra/doc/vector_merge_efficiency_analysis.md`
against primary evidence.

---

## 1. Corroboration of the efficiency analysis (claim → primary evidence)

| claim (doc §) | primary evidence | status |
|---|---|---|
| ordinal pass pre-fix **205 µs/node, 1 worker** (§2) | `Similarity ordinals assigned: 3966037 … in 814379 ms` @ 00:06:44 → 205.3 µs | **confirmed exactly** |
| pre-fix 16M: 200 µs/node | `15864356 … in 3172998 ms` @ 00:29:30 → 200.0 µs | **confirmed** |
| `ab90f90e` alone: 54.7 µs/node | `6951603 … 7 sources in 380138 ms` @ 01:02:21 | **confirmed** |
| post-fix **10.4 µs/node** | `8859935 … 9 sources in 92119 ms` @ 01:14:29 | **confirmed** |
| post-fix shared-pool 16.7 µs | `35694525 … in 597124 ms` @ 01:34:57 | **confirmed** |
| post-fix 9-src merge **20.5 min (2.31 min/M)** | ordinal pass ends 01:14:29; `TERMS_DATA written in place (9 source segments, 8859935 …)` @ 01:33:44 | **confirmed** (~20.5 min) |
| `retain_largest` flag **inert** on deployed jar (§3) | `deployed jvector has no retain-largest control` logged per merge (01:20:39, 01:34:55) | **confirmed** |
| retained-only fast path ~1% (§3) | `39005 / 38969 / 35915 retained-only` of ~3.97M | **confirmed** |
| cluster search ~0.003% effective (§3) | this session's independent 183-report scan: 0.0024–0.0032% over 1.18B searches, 1.98 resumes/node | **confirmed independently** |
| STCS(4) → four near-equal sources (§4) | this session's tier histograms (sources within 27 nodes of each other) | **confirmed independently** |
| 01:05 drain failure (§6) | `IllegalStateException: Nodes instance already closed` ×2 in system.log | **confirmed** (replay row-counts not re-verified; phrasing differs) |
| `BoundedParallelExecutor.java` untracked while `4c29e7c6ee` references it (§6) | `git status` `??` (source + test), 0 tracked copies, commit references it ×2 | **confirmed — clean-clone build break** |
| base layer 0.66 vs 1.73 min/M = T×(k−1) (§2) | doc-sourced phase splits; adjacent numbers all verify | consistent, not independently recomputed |
| retain-largest 0.5 regime gate / recall 0.956→0.792 (§4) | measured on `jvector-experiments`, not reproducible here | branch-doc-sourced |

Every independently checkable number checked out. The analysis is trustworthy.

## 2. The stack, as deployed right now

| | |
|---|---|
| node | up 03:32:05, pid 3081002; table absent (`baselines` recreated by the client) |
| rig | `MemoryHigh=146G` drop-in intact (~38–47 GiB page cache; above-RAM at ~70 min) |
| jvector | `experiment/cluster-rescore-prefetch-20260824` @ **`7de94e83`**, tree clean, jar md5 `33f1202ccd8ca4e3696f7004e565ecfe` (built 01:06) |
| cassandra | `experiment/effective-remappers-readback-20260825` @ **`4c29e7c6ee` + 29 uncommitted files** (incl. untracked `BoundedParallelExecutor{,Test}.java`), jar built 01:06 |
| nb-rs | `6b47a42` (saturation breaker + example + report block in the workload) |

**Branch topology, verified by ancestry** (`merge-base --is-ancestor`): the intended
layering is intact —

```
integration-robustness (d55d0d57)      ← seams; upstream PR 1
  ├─ compaction-conversion (d6a3f68e)  ← upstream PR 2
  └─ xlink-integration (117e856f)      ← xlink work on top of the seams
       └─ experiment/cluster-rescore-prefetch-20260824 (7de94e83)   ← DEPLOYED
```

**Flags live on the JVM (6):** `enable_native_vectorization`, `mode=production`,
`sourcePretouchMaxNodes=-1`, `sourcePretouchWindowNodes=1048576`,
`similarityOrdinals=true`, **`clusterSearch=false`** ← the Run C arm.

## 3. Run C — definition and expectations

**Decision (2026-08-26): symmetric merges are the practical reality; optimize that
path first.** STCS(4)'s equal sources hold base share at 25%, below retain-largest's
0.5 regime gate, and the same shape defeats the deployed elisions (cluster search
0.003%, retained-only 1%).

Run C = the overnight-prepared post-fix stack **+ `clusterSearch=false`** (one flag).
It removes ~40 wasted full-precision rescores/node (~245M reads per 4-src merge) from
the base layer, which post-fix is 75% of merge wall. Cross-source L0 becomes one plain
search per node — the clean `T×(k−1)` term.

Verify per merge: `Cluster path cost: enabled=false`, near-zero exact rescores, and
`adopted effective ordinal mapping … (REORDERED by the compactor)`.
References: post-fix baseline 2.31 min/M (9-src, cluster ON); pre-fix collapse-band
numbers no longer comparable.

The cluster-ON post-fix control (also the efficiency doc's §5 discriminator — one 16M
merge solo) remains one flag-flip away if the delta needs tightening.

## 4. The code track queued behind Run C

Port **O4 → O7 → O1** from `livenodes-integration` (`45e088a4` 918 ins, `9c213592`
190 ins, `c74c2b06` 472 ins) as a **conversion branch off `xlink-integration`** —
NOT off the experiment tip — re-expressed through the robustness seams
(ParallelExecutor / ProgressLimiter / destination), then merged under the experiment
branch for deployment. Rationale: O4 kills retrain+re-encode (~14% of merge);
O7 kills same-source full-precision reads (measured on its branch as 86% of the
remaining FP reads); O1 multiplies once O4 makes codebooks identical. The Cassandra
receiving side re-wires what `6e1e437689` stripped, under the required-explicit
flag regime.

## 5. Preconditions / debts

1. **Commit the Cassandra working tree** (29 files + the two untracked
   BoundedParallelExecutor files). Until then `4c29e7c6ee` does not compile from a
   clean clone, and every run inherits a provenance hole.
2. Re-home `fb9326a7` (drain guarantee) — and likely `ab90f90e` — onto
   `integration-robustness` before the upstream PRs go out; they are robustness-class
   fixes currently living on the experiment branch.
3. Superseded conclusions stay superseded: Run B2's "+2.2% / similarityOrdinals no
   effect" was confounded by the single-threaded ordinal pass; pre-fix merge-rate
   comparisons must not be cited against post-fix runs.
