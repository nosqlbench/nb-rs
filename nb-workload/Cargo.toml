[package]
name = "nb-workload"
version = "0.1.0"
edition = "2024"
description = "Uniform workload specification parsing and processing for nb-rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1"
regex = "1"
